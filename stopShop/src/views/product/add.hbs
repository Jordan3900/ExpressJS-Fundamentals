<div class="form-group">
    <label for="group">
      Group
    </label>
    <select class="input-field" name="category">
      {{#each categories}}
      <option value="{{this._id}}">{{this.name}}</option>
      {{/each}}
    </select>
  </div>
